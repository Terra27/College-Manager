package com.example.collegemanager.home;

// This is a class that defines each Option in the Home Window.
public class HomeOptions {
    String optionTitle;
    int optionPicture;
    public HomeOptions(String title, int src) {
        optionTitle = title;
        optionPicture = src;
    }
}